APP_DATABASE_URL=postgres://root@127.0.0.1:26257/das?sslmode=disable
APP_SERVER_PORT=9090
# TibaneLabs fork: loopback is the default; set explicitly only to widen deliberately.
APP_SERVER_HOST=127.0.0.1
